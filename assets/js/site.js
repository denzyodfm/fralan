const menu=document.querySelector('.menu-button'),nav=document.querySelector('.site-header nav');menu?.addEventListener('click',()=>nav.classList.toggle('open'));const filterButtons=document.querySelectorAll('.filters button');const searchInput=document.getElementById('archive-search');const archiveCards=document.querySelectorAll('.archive-card');const archiveEmpty=document.getElementById('archive-empty');let activeFilter='all';const applyArchiveFilters=()=>{const query=(searchInput?.value||'').trim().toLowerCase();let visibleCount=0;archiveCards.forEach(card=>{const type=(card.dataset.type||'').toLowerCase();const text=(card.textContent||'').toLowerCase();const matchesFilter=activeFilter==='all'||type===activeFilter;const matchesQuery=!query||text.includes(query);const visible=matchesFilter&&matchesQuery;card.hidden=!visible;if(visible)visibleCount++;});if(archiveEmpty)archiveEmpty.hidden=visibleCount!==0;};filterButtons.forEach(button=>button.addEventListener('click',()=>{filterButtons.forEach(b=>b.classList.remove('active'));button.classList.add('active');activeFilter=button.dataset.filter||'all';applyArchiveFilters();}));searchInput?.addEventListener('input',applyArchiveFilters);const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting)entry.target.classList.add('visible')}),{threshold:.08});document.querySelectorAll('.archive-card,.journal-list>a,.intro>div').forEach(el=>observer.observe(el));
